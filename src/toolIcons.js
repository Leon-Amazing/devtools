import { AlignLeft, Binary, Braces, Clock3, Fingerprint, Hash, Link2, Regex } from 'lucide-vue-next'

export const toolIcons = {
  json: Braces,
  base64: Binary,
  url: Link2,
  timestamp: Clock3,
  uuid: Fingerprint,
  hash: Hash,
  text: AlignLeft,
  regex: Regex,
}
